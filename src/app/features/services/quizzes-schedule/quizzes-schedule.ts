import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-quizzes-schedule',
  imports: [RouterLink],
  templateUrl: './quizzes-schedule.html',
  styleUrl: './quizzes-schedule.css',
})
export class QuizzesSchedule {}
